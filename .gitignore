# Node / JavaScript
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
/dist/
/build/
/tmp/
/out/
/coverage/
/*.log
*.tgz

# Environment
.env
.env.*.local
.env.local
!.env.example

# TypeScript / cache
*.tsbuildinfo
.cache/
.parcel-cache/
.eslintcache
jest-cache/
.vitest/

# Next.js (web)
.next/
.next/cache/
out/
.vercel/
.vercel_build_output/

# React Native (mobile)
# Android
/android/.gradle/
/android/app/build/
/android/build/
/android/local.properties
**/gradle-wrapper.jar
**/gradle-wrapper.properties

# iOS
/ios/Pods/
/ios/build/
/ios/DerivedData/
/ios/*.xcuserstate
*.mobileprovision
*.p12
*.cer
*.keystore
debug.keystore

# Metro / packager
/metro-cache/
/packager-info.json

# CocoaPods
# (keep Podfile.lock if you commit it; Pods/ should be ignored)
Pods/

# Build outputs that may appear in multiple projects
dist/
lib/
esm/
cjs/

# Editor / OS
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
.DS_Store
Thumbs.db
desktop.ini

# Git / misc
*.lock
*.log
npm-debug.log*

# Optional: lockfile policies (uncomment if you want to ignore other package manager lockfiles)
# yarn.lock
# package-lock.json
# pnpm-lock.yaml

# Secrets and credentials (common patterns)
*.env.local
*.secret
credentials.json